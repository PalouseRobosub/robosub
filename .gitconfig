#note: to make this settings file active, you need to run
#git config --local include.path ../.gitconfig


#only allow fast-forward merging
[merge]
	ff = only

#normalize line endings automatically when files are commited
[core]
	autocrlf = input
